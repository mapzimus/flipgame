# Flipgame v1.12 defect ledger

The release gate is zero known open P0/P1/P2 defects. `Pending` means the item
is part of v1.12 and is not yet eligible for release.

| ID | Severity | Subsystem | Reproduction | Status | Verification required |
|---|---|---|---|---|---|
| V112-001 | P1 | Progression/UI | Win and receive an object reveal, then open Customize without reloading; the item can remain absent | V3 recovery fixed (`4349a62`); audit confirms V4 is not live-wired and remains open | One authoritative V4 live store, immediate selection, FL1–100 migration/no-relock |
| V112-002 | P1 | Input/Physics | A short fast flick displays near 25% while launching with peak velocity/full rotation | Fixed (`d765973`, `193f819`); independent QA passed at `af6c4c4` | Meter/launch signal parity across pointer types |
| V112-003 | P1 | Physics | Ordinary playtest objects rotate substantially faster and feel harder than intended | Fixed (`d765973`); independent QA passed at `af6c4c4` | Deterministic low/mid/high calibration and event regression |
| V112-004 | P2 | Unlock UI | Locked/reveal tile retains the prior selected object's pixels beneath `?`/`🔒` | Independently reproduced; implementation pending | Clean canvas lifecycle, queue, secrecy screenshots |
| V112-005 | P2 | Progression/UI | Odd-win visual/cosmetic reward is persisted but produces no reveal notification | Independently reproduced; object-only filter must be replaced | Every reward type and queued mixed rewards |
| V112-006 | P2 | Customize | Variant tab shows color dots/generic names; actual cast appears only after returning to Setup | Independently reproduced; canonical V111/V112 variant mapping must be frozen first | All objects/variants, unique names, immediate art preview |
| V112-007 | P2 | Owner testing | No safe way exists to preview all locked content without changing earned progression | Audit confirms exact mode has no runtime implementation | Exact temporary code, immutable earned profile and Test Data boundaries |
| V112-008 | P2 | Plinko | Playtest view appears seven slots wide and the fall is too short for the expanded board | Pending | Nine-slot render, longer board, tracked camera, deterministic outcomes |
| V112-009 | P1 | Physics/Alien | Classic calibrates near 32.5% while temporary Alien is near 68% and native Alien reaches about 82% before dropping at 4K | Fixed (`d765973`, `2996a8c`, `af6c4c4`); independent QA passed | Alien within ±10pp of matched Classic per viewport; Alien viewport spread ≤10pp; CPU bands ordered |
| V112-010 | P1 | Events | Some events are visually too subtle to understand during play | Audit assigned | 30-event physical/visual/audio/camera matrix and owner approval |
| V112-011 | P1 | Events | Earlier assistance profiles may be guaranteed or effectively automatic over broad inputs | Audit assigned | Non-zero miss path and mixed-outcome deterministic corpus per event |
| V112-012 | P1 | Art/Renderer | Smile/frown/scared overlay is painted over the authored neutral face, doubling facial features | Pending | Single-face states across every allowlisted variant/legacy object; Bottle/T-Rex exclusions |
| V112-013 | P1 | Art | Product owner rejected the current new-object artwork as substantially below the v1.12 quality bar | Seven-Flipper calibration implemented (`0561188`); owner approval and full roster pending | Approved calibration set, 25-object gallery, 300-variant motion/screenshot review |
| V112-014 | P1 | UI/UX | Product owner rejected the responsive menu as robotic and boring | Open | Approved concept direction and code-native phone/smartboard prototype before rollout |
| V112-015 | P1 | Input/Physics | Equivalent intentional flicks feel much harder on phone than desktop | Fixed (`d765973`, `193f819`); independent QA passed at `af6c4c4` | Timestamped/coalesced gesture replay and bounded cross-device make-rate parity |
| V112-016 | P1 | Alien/Renderer | Floating UFO bank surfaces are missing in observed Alien and Alien Invasion play | Fixed (`af6c4c4`, `e938af2`); independent re-audit passed | Exact authoritative transform parity and phone/desktop/4K browser evidence |
| V112-017 | P2 | UI/Renderer/Audio | ON FIRE is no longer visually pronounced enough to read as a major momentum state | Open | Escalating lifecycle, cleanup, reduced-motion, phone/smartboard evidence |
| V112-018 | P1 | Events/Renderer | Mitosis shows a generic/cap-like secondary body instead of two complete selected-object clones | Open | All objects/variants, independent dynamics, conserved physics, one/both/neither outcomes |
| V112-019 | P1 | Platform/UI | Online UI and runtime networking remain in the v1.11-derived shipped graph despite the v1.12 offline-only decision | Open | No route, loaded/cached module, socket/transport, or reachable state path; legacy imports preserved |
| V112-020 | P2 | Player scaling | Setup, modes, Mirror Match, saves, stats, and HUD contain hard-coded eight-player bounds or truncation | Open | Central limits and exhaustive supported-count lifecycle tests |
| V112-021 | P1 | Battle/Input | The current singleton input/physics model ignores additional pointers and cannot isolate simultaneous Battle lanes | Implemented (`36a3b70`, `c2c78e9`); hardware and independent QA pending | Two/four-contact calibration, lane isolation, simultaneous settlement, performance, and fallback |
| V112-022 | P1 | CPU/Difficulty | Classic Medium/Hard CPUs make about 73%/84%; Alien can reach about 85% on Easy and reverse tier ordering | Live wiring/lifecycle fixed; candidate `ce41470` rejected by a disjoint unseen corpus: Alien Easy/right 1920×1080 was 29.34% and Hard/right 3840×2160 was 59.51% | Easy 30–40%, Medium 45–55%, Hard 60–70%; raw ordered bands per mode/direction/viewport/player count on primary, holdout, a new unseen corpus and release-scale >=10,000/cell evidence |
| V112-023 | P1 | Art/Globe | Desk Globe can appear as a fixed Africa-facing illustrated disc rather than a visibly complete rotating real Urth sphere | Calibration implemented (`0561188`, `da218f8`, `634c21a`); integration/QA pending | Realistic literal sphere only; 360° cycle, focus detail, offline/cache/APK, performance, unchanged collider |
| V112-024 | P2 | UX/Arenas | Arena choice lacks the required dedicated fighter-then-stage screen and the collection has only the ten inherited themes | Planned | Full Arena Select flow, 22-stage ownership/migration/reveals, hidden locks, rematches, responsive/Battle evidence |
| V112-025 | P1 | Battle/Fairness | Rush power offers use the global result count, so reversing simultaneous lane settlement can change which cards each competitor receives | Fixed (`ec3ab2d`); independent audit passed | Per-competitor deterministic offer streams invariant to launch/settle/callback order |
| V112-026 | P1 | Battle/Powers | A symmetric Round card is queued once per team and can be consumed by only the first of two simultaneously active teammates | Fixed for single-card delivery (`ec3ab2d`); stacking audit remains V112-051 | All active eligible representatives receive the same round effect exactly once; relay semantics covered |
| V112-027 | P1 | Battle/Sudden Death | A tied four-way heat can re-admit players who were behind when paired sudden death begins | Fixed (`ec3ab2d`); independent audit passed | Only tied leaders enter paired sudden death; repeated ties and two/four/relay hardware covered |
| V112-028 | P2 | Battle/Powers | A targeted card cannot be selected and stored while its target is aiming, although only deployment should be time-gated | Fixed (`ec3ab2d`); independent audit passed | Selection remains legal; deployment after any affected pointerdown remains rejected |
| V112-029 | P1 | Battle/Runtime | Adapter launch/promise/callback failures can leave lanes or synchronized gates permanently inflight | Fixed (`b956db2`, `3652061`); independent re-audit passed | Deterministic error cleanup for synchronous throws, rejected/native/hostile promises and observer callbacks without double resolution |
| V112-030 | P1 | Battle/Rotation | Larger teams can finish a 2–0 Battle before several teammates are ever assigned because the rotation cursor advances by one even when two representatives play | Fixed (`b956db2`); independent re-audit passed | Coverage/fairness matrices for 3v3–8v8 on one-, two- and four-touch hardware across Volley/Rush and heat starts |
| V112-031 | P1 | Battle/Input | A Rush player can hold an aim across 15-second rotation boundaries, launch after losing the seat, and starve later teammates | Fixed (`3652061`); independent 18-case re-audit passed | Unlaunched aim cancellation at the first crossed rotation boundary with exactly-once power restoration |
| V112-032 | P1 | Battle/Clock | Horn eligibility depends on delayed `tick()` ordering; a stalled frame or CPU path can continue well beyond the absolute deadline | Fixed (`b956db2`); independent re-audit passed | Absolute heat deadline, stalled-frame reconciliation, CPU cutoff, and pre-/post-horn release races |
| V112-033 | P2 | Battle/CPU | Runtime recognizes `cpu`/`isCpu` but not the current setup model's `ai` flag | Fixed (`b956db2`); independent re-audit passed | Mixed human/CPU normalization and launch routing from legacy/current setup shapes |
| V112-034 | P1 | Battle/Relay | One-touch 1v1 Timed Rush marks both duelists active while only the first has a physical lane, so the second cannot flip | Fixed (`b956db2`); independent re-audit passed | Alternating one-lane duel assignments, equal clock exposure and complete runtime heat |
| V112-035 | P1 | Rules/Idempotence | Reusing a resolved `flipId` mutates Classic, Cup, shootout, and Team state again while emitting a duplicate outcome ID | Fixed (`fc8d56a`, `61ac9b0`); independent 10k re-audit passed | Compact caller-bound monotonic identity; whole-match duplicate/stale rejection across every format without growing payload |
| V112-036 | P1 | Rules/Finalization | Active rules state can be reported as completed, while a completed state overridden to abandoned/cancelled retains winner data | Fixed (`fc8d56a`); independent re-audit passed | Boundary status/phase validation and winner/reason stripping for every non-completed outcome |
| V112-037 | P1 | Rules/Story CPU | Story's `kind: cpu`, `human: false`, `displayName`, and `cpuTier` shape normalizes as an unnamed human | Fixed (`82f1639`); independent audit passed | Real Story request normalization, tier retention, and all legacy CPU aliases |
| V112-038 | P1 | Rules/Story co-op | Opponent-targeted effects can halve or drain the allied Story co-op human because the structural targeting policy is discarded | Fixed (`82f1639`, `fc8d56a`); independent re-audit passed | Actor-scoped alliance protection without per-flip caller repair |
| V112-039 | P2 | Rules/Cup presentation | A nonfinal Cup heat copies the inner Classic `match-win` cue despite the Cup remaining active | Fixed (`82f1639`); independent audit passed | Heat-only versus final-Cup positive cue matrix |
| V112-040 | P2 | Rules/Sudden death | External elimination of a player who owes the final turn in a sudden-death band delays escalation and gives an extra lower-penalty turn | Fixed (`82f1639`); independent audit passed | Elimination at every band boundary, direction, and active-player count |
| V112-041 | P2 | Rules/Validation | Forged schema-tagged Classic or Team configs bypass normalization and can create active zero-player states | Fixed (`82f1639`, `fc8d56a`, `61ac9b0`); independent re-audit passed | Revalidated public config/state paths plus reachable regulation/sudden-death invariants |
| V112-042 | P2 | Rules/Cup lifecycle | A forced all-player elimination completes the inner Classic heat with no survivor but leaves the outer Cup permanently stuck | Fixed (`82f1639`); independent audit passed | Deterministic fair no-survivor tie resolution and continued Cup lifecycle |
| V112-043 | P2 | Training/Statistics | Ordinary Practice classification was repaired, but dynamically forced or cleared force state does not propagate correctly through coordinator finalize/abandon | Fixed (`4cd20be`, `e709044`); independent re-audit passed | Attempt-accurate classification plus mandatory provider-backed finalization/abandon propagation for ordinary, forced, cleared, and mixed sessions |
| V112-044 | P2 | Training/Event names | Practice does not pass the active typed name to event selection, so force names and exact `Mr. Howe` have no effect without a separate UI call | Fixed (`2e2591d`); independent audit passed | Exact roster-name forcing and Mr. Howe weighting through the runtime, with case-sensitive controls |
| V112-045 | P2 | Training/Lab gate | Lab entry paths now request authorization, but a hand-built structural `PhysicsLabAuthorizationV1` token can bypass FL50 | Fixed (`4cd20be`); independent re-audit passed | Issuer-authenticated token plus independently gated direct Activity request and migrated entitlement |
| V112-046 | P2 | Training/Seeds | Fresh zero/one seeds are distinct, but a restored Tutorial request reports the constructor seed instead of the persisted state seed | Fixed (`4cd20be`); independent re-audit passed | Distinct zero/one streams and fresh/restored request-state-attempt seed parity |
| V112-047 | P2 | Training/Memory | Long Training sessions retain an unbounded replay map and full trajectories of up to 1,200 samples each | Fixed (`2e2591d`); independent audit passed | FIFO 256 full entries, deterministic 480-sample trajectories, 24 ghosts, and evicted replay rejection |
| V112-048 | P2 | Tutorial/Pacing | The Tour can stall indefinitely on a mandatory cap landing and delays the temporary T-Rex until after the first event showcase | Fixed (`2e2591d`); independent audit passed | Bounded fallible cap guidance, protected T-Rex before both events, and 45–75-second completion corpus |
| V112-049 | P1 | Battle/Relay clock | A shot launched just before a one-lane Rush handoff can occupy the lane through the opponent's entire scheduled window | Fixed (`b956db2`); independent re-audit passed | Equal ready-and-assigned opportunity time; cross-boundary resolution pauses/compensates the incoming allocation |
| V112-050 | P1 | Battle/Volley fairness | One-lane Equal Volley always lets the same side move first and use a newly earned hostile card against the opponent in that same logical volley | Fixed (`b956db2`); independent re-audit passed | Rotating opener and offers/deployment deferred until the complete paired volley resolves |
| V112-051 | P2 | Battle/Powers | Multiple physical event adapters can stack on one recipient's next launch with no compatibility or ordering contract | Fixed (`b956db2`); independent re-audit passed | One pending physical modifier per recipient; atomic rejection preserves the stored card and existing queue |
| V112-052 | P1 | Activity/Finalization | Shared `MatchOutcomeV2` and the coordinator still accept an active rules state as completed and can preserve winners on a non-completed status | Fixed (`fc8d56a`); independent re-audit passed | Boundary-level status/phase validation and winner/reason sanitization independent of helper call path |
| V112-053 | P2 | Rules/Cup validation | A schema-valid Cup state can embed an unrelated Classic heat or impossible heat/history/win totals | Fixed (`fc8d56a`, `61ac9b0`); independent re-audit passed | Full inner match/config/starter/life/sudden-death consistency and reachable phase/history/clinching invariants |
| V112-054 | P2 | Rules/Team validation | A forged Team queue can assign an actor to the opposing team or repeat one actor for all six entries | Fixed (`fc8d56a`, `61ac9b0`); independent re-audit passed | Exact canonical queue, actor totals, position/round/team/player and automatic-result validation |
| V112-055 | P2 | Rules/Classic validation | Classic accepts stale derived turn metadata, inflated lives, and orphan ON FIRE flags in schema-tagged state | Fixed (`fc8d56a`, `61ac9b0`); independent re-audit passed | Recomputed derived state, bounded legal lives, coherent ON FIRE ownership and reachable sudden-death invariants |
| V112-056 | P1 | Rules/Story co-op | Co-op clear condition is discarded, so surviving allies must fight; blanket ally protection also shields humans from CPU opponent effects | Fixed (`604f15b`, `fc8d56a`); independent re-audit passed | Exact Story ally sets, actor-scoped targeting and completion when all survivors belong to the allied clear set |
| V112-057 | P2 | CPU/API | CPU seed validation coerces `null`, strings and booleans into valid seeds despite advertising an unsigned-integer contract | Fixed (`e599ce6`); independent re-audit passed | Strict primitive uint32 validation, adversarial invalid-input matrix and deterministic non-prefix windows |
| V112-058 | P2 | Alien/Lifecycle | Direct Alien event cleanup removes geometry but leaves public bank telemetry populated until the next full bottle reset | Fixed (`e599ce6`); independent re-audit passed | Immediate telemetry/cooldown/trace cleanup on every exit, next-shot isolation and replay/resize coverage |
| V112-059 | P1 | Progression/Persistence | A storage adapter that writes and then throws can return `persistence-failed` while the uncommitted profile becomes active after restart | Candidates `3cbf6718` + `951ca52` rejected; second remediation active | Exact readback commit or journaled rollback leaves the return value, memory, stored bytes and restarted state identical even when rollback also fails |
| V112-060 | P1 | Progression/Subscriptions | Reentrant commits can deliver revision 2 before revision 1 to later subscribers and listeners added mid-dispatch can amplify one event | Corrected in `951ca52`; independent second audit passed this item; awaiting accepted domain merge | Queued monotonic delivery, stable listener snapshot, exception and unsubscribe safety |
| V112-061 | P1 | Progression/Import | A checksummed profile can import inconsistent reward/processed claims that suppress future legitimate object or match rewards | Candidates `3cbf6718` + `951ca52` rejected; second remediation active | Imported achievement/rival evidence carries exact reward provenance or cannot suppress a later authoritative local claim |
| V112-062 | P1 | Progression/Reveals | Reimporting equivalent profile content with a different document timestamp can resurrect a dismissed reveal | Corrected in `951ca52`; independent second audit passed outside the Store namespace defect V112-076 | Durable consumed-reveal identity; stale/equivalent imports are idempotent and new reveals appear once |
| V112-063 | P1 | Progression/Refresh | A higher-revision external V4 snapshot can replace local progress, relock content and update no subscribers | Corrected in `951ca52`; independent second audit passed ordinary monotonic refresh; cross-writer conflict is V112-078 | Strict lineage/monotonic merge, no downgrade and exactly-one ordered notification |
| V112-064 | P2 | Progression/Storage | Processed match IDs, FC transactions and feedback can grow without a retention/compaction bound until local storage fails | Corrected in `951ca52`; independent second audit passed sequential exact bounds; awaiting accepted domain merge | Bounded durable idempotence/ledger/queue with no duplicate rewards or no-op dismissal claims |
| V112-065 | P2 | Progression/Validation | Public mutation and inner backup fields coerce numeric IDs and string amounts despite exact-schema contracts | Candidates `3cbf6718` + `951ca52` rejected; second remediation active | Strict primitive, exact-own-key identifier, state, token, reward and transaction matrix |
| V112-066 | P2 | Progression/Safety | Exported setup migration/merge helpers can return prototype-bearing objects from hostile parsed keys | Corrected in `951ca52`; independent second audit passed; bounded traversal remains V112-081 | Recursive unsafe-key rejection and null-prototype/own-property copying |
| V112-067 | P1 | Events/Authority | Structural lookalike runtime, renderer and rules capabilities are accepted; a cloned non-issued outcome can award a reward | Corrected in integrated candidate `61f88c5`; integrator adversarial/static audit passed, independent post-integration audit pending | Module-private branded capabilities; fake authority and cloned outcome rejection at every boundary |
| V112-068 | P1 | Events/Idempotence | One genuine event outcome can resolve once through each unrelated adapter/match because consumption is adapter-local | Corrected in `61f88c5`; full local matrix passed, independent audit pending | Root-atomic one-shot consume bound to match namespace, format, lane, launch claim and resolution identity |
| V112-069 | P1 | Events/Rules identity | Publicly recomputable identity checksum is treated as authority and the same lane/seed/event creates the same launch claim | Corrected in `61f88c5`; live high-water and one-use claim probes passed, independent audit pending | Live rules high-water capability, unique one-use claim and exact caller/lane/resolution binding |
| V112-070 | P1 | Events/Format | Caller selects `formatId` per resolution, allowing Classic mapping in a Team namespace and bypassing Team exclusions | Corrected in `61f88c5`; immutable-format/Team exclusion probes passed, independent audit pending | Match-bound immutable format validated against rules-owned namespace/state |
| V112-071 | P1 | Events/Renderer | Colliderless physical entities, omitted bodies, frame-time body mutation and double frame callbacks can diverge rendering from physics | Corrected in `61f88c5`; collider parity/reduced-motion probes passed, independent audit pending | One pure collider-derived frame; every physical entity represented; before/after body parity; reduced motion derived without a second gameplay call |
| V112-072 | P1 | Events/RNG | Captured ambient RNG bypasses guards and rendering advances the shared gameplay RNG stream, changing later physics | Corrected in `61f88c5` within the enforceable authored-module boundary; deterministic replay/dynamic guards passed, independent audit pending | Separate gameplay/counterless visual streams, render counter invariance and deterministic replay/lint probes |
| V112-073 | P1 | Events/Rewards | Identical Plinko or Roulette geometry accepts conflicting supplied slot/sector indices and therefore different payouts | Plinko re-audit reproduced conflicting slot claims despite prior tests; canonical geometry remediation active | Host-owned sensor metadata and geometry-derived Plinko slot/Roulette sector with contradictory-fact rejection |
| V112-074 | P1 | Rules/Plinko | Classic/Cup automatic loss/win metadata does not execute a terminal rules transition; ON FIRE can mask automatic loss | Corrected in `61f88c5`; Classic/Cup/Team/ON FIRE terminal matrix passed, independent audit pending | Explicit idempotent rules-owned terminal path for Classic match, Cup heat and Team match; ON FIRE cannot intercept |
| V112-075 | P2 | Events/Plinko | Kernel accepts only 10–15 second drops and cannot represent the contracted 9–18 second tail, 22-second recovery or 30-second no-contest | Corrected in `61f88c5`; 9/18/22/30-second boundary and same-turn retry probes passed, independent audit pending | Distinct clean/recovery/no-contest timing and provenance through runtime, rules, stats and cleanup |
| V112-076 | P1 | Progression/Store | One cosmetic purchase queues both `cosmetic.<id>` and `store.<id>`, but validation accepts only the latter, making the profile impossible to export or reload | Candidate `951ca52` rejected; remediation active | Exactly one canonical Store reveal for each of 40 purchases; validate, export and restart remain lossless |
| V112-077 | P1 | Progression/Activity | A Rival Board reservation can receive ordinary match FXP/FC when caller reward data omits or lies about activity | Candidate `951ca52` rejected; remediation active | Reward activity derives from the immutable reservation; Rival Board ordinary rewards are always zero |
| V112-078 | P1 | Progression/Concurrency | Two same-origin writers can reserve and consume ordinal 1 at the same revision, return `applied:true`, and retain divergent balances | Candidate `951ca52` rejected; lifetime Web Lock writer boundary plus lower-layer conflict defense assigned | Only one browser writer; reentrant/two-context interleavings issue and consume one ordinal once; equal-revision divergence fails closed |
| V112-079 | P1 | Progression/Authority | Raw public achievement, rival and Story mutators can mint rewards without authoritative gameplay and bypass Owner Test | Candidate `951ca52` rejected; branded authority/catalog remediation active | Canonical 120-ID achievement rarity plus coordinator-bound evidence; no browser-facing raw/default reward bypass |
| V112-080 | P2 | Progression/Reservation | `another-match-active` returns the active match's bearer token, allowing a conflicting caller to consume or abandon it | Candidate `951ca52` rejected; remediation active | Conflict response contains no token; resume remains bound to the owning coordinator/session |
| V112-081 | P2 | Progression/Safety | Recursive clone and unbounded dismissal/Story arrays can stack-overflow or allocate excessively before semantic rejection | Candidate `951ca52` rejected; remediation active | Explicit depth/node/item budgets reject hostile payloads deterministically without partial mutation |
| V112-082 | P1 | Rules/Events authority | The live Rules adapter exposed its genuine event match capability, allowing a caller to bypass collider-derived kernel evidence | Fixed in `61f88c5`; capability is closure-private and event authority is one-shot | No public property or repeat call can obtain the Rules event capability; structural substitutes fail |
| V112-083 | P1 | Events/Idempotence | A duplicate Mirror Match resolution re-emitted its actionable deferred effect even though the rules transition was a duplicate | Fixed in `61f88c5`; duplicate deferred/rules/terminal outputs are inert | One outcome can arm a deferred effect at most once |
| V112-084 | P1 | Rules/Authority | The ordinary live flip method accepted event effects, raw Team points and forced elimination without using the event authority chain | Fixed in `61f88c5`; strict ordinary input rejects event-only fields | Event rewards and terminal effects enter live state only through branded event consumption |
| V112-085 | P1 | Events/Mirror persistence | Mirror Match's next-flip effect is returned as deferred adapter output but is not yet atomically represented in the v1.12 persisted match session | Open; whole-game coordinator wiring not started | Crash/reload between resolution and arming cannot lose or repeat a Mirror Match obligation |
| V112-086 | P1 | Physics/Rules authority | Ordinary live rules input is structurally validated but is not yet a physics-issued branded `LandingVerdict` | Open; physics-to-rules bridge wiring required | Only one settled host verdict can resolve one ordinary live flip; forged/stale/copied verdicts fail |
| V112-087 | P1 | Progression/Browser authority | A caller can capture the raw Profile store and writer toggle through `connectProductionRuntime` before the intended Runtime boots | Remediation active; browser connector must be absent | Browser exports contain no raw store, toggle, connector or mutation path before/after boot |
| V112-088 | P1 | Progression/Live composition | Runtime creates a private reward authority but the browser connection discards it, leaving no legitimate live reservation/achievement path | Open; dedicated lexical composition required | One real browser match and achievement finalize through private authority; UI receives redacted projection only |
| V112-089 | P1 | Progression/Receipts | A higher revision can clear an active match reservation and advance the consumed frontier without appending its exact receipt, allowing the same match ID to reserve again | Remediation active | Exact contiguous/rolled receipt frontier; active-to-consumed transition binds match/activity/ordinal and rejects replay |
| V112-090 | P1 | Progression/Multi-tab refresh | A valid external-evidence-only import is not counted as a semantic change, so another tab rejects the higher revision and permanently closes | Remediation active | Two-tab evidence-only import refreshes once without downgrade, poison or missed notification |
| V112-091 | P1 | Achievements/Authority | The browser achievement evaluator can issue genuine branded reward evidence from fabricated public context | Fixed in isolated candidate `e425747`; independent re-audit/merge pending | Browser exposes catalog-only facade; evaluator/authority remain lexical and fabricated facts cannot mint evidence |
| V112-092 | P1 | Progression/Achievement replay | A rewarded achievement whose FC transaction was compacted can be paid again after its processed-claim correlation is removed | Remediation active | Rewarded ID requires exact processed evidence; direct duplicate guard survives reload and compaction |
| V112-093 | P2 | Progression/Import | Unknown achievement IDs can fill the 120-entry bound and prevent legitimate achievements from being granted | Remediation active | Known aliases canonicalize/dedupe; unknown IDs cannot enter production state or consume capacity |
| V112-094 | P1 | Runtime/Module identity | A schema-only preseeded browser global can bypass Profile/Runtime initialization and a browser `module` shim can be misclassified as trusted CommonJS | Candidate `d9f97de` rejected by independent audit; true-Node classification still required | CommonJS ignores globals; browser duplicate/preseed/module-shim cases fail closed without invoking `require` |
| V112-095 | P2 | Progression/Writer handoff | A tab that initially finds the Web Lock busy may remain queued; closing that queued runtime does not abort its retained request or immediately publish `closed` | Candidate handoff succeeds, but queued-close lifecycle remains open | Existing second tab queues/retries, refreshes before takeover; close aborts a queued request, disables writing, settles readiness and publishes `closed` immediately |
| V112-096 | P2 | Progression/Receipt retention | The exact match receipt ledger disables all future reward-bearing matches at 4,096 completions | Open; collision-resistant summary/rollover required | Boundary and long-life simulation preserve replay rejection without permanent reward shutdown |
| V112-097 | P2 | Progression/V4 import exactness | Imported V4 can omit lineage/ledger metadata or attach extra/accessor array properties and share an `unassigned-v4` namespace | Remediation active | Exact top-level and dense-array descriptors, mandatory unique lineage/frontiers and no getter invocation |
| V112-098 | P1 | Android/Progression writer | APK progression assumes Web Locks work on `file:///android_asset/` without device evidence or an implemented native adapter | Open device gate | Real supported WebView grants exactly one durable writer or a tested native coordination adapter is used |
| V112-099 | P1 | Battle/Rush evidence | Terminal Battle validation can prove a player appeared during a Timed Rush heat but not that each attempt occurred in that player's exact assignment window | Remediation active | Every Rush attempt carries a bounded launch lease; wrong-bucket/forged/post-horn evidence is rejected |
| V112-100 | P1 | Battle/Activity authority | Activity re-reads a mutable `FlipgameV112Battle` global on every outcome, so replacing it after legitimate load can authorize fabricated Battle terminal evidence | Remediation active | Immutable/preseed-rejecting Battle export plus one-time authority capture; replacement and cross-instance cases fail |
| V112-101 | P1 | Rules/Activity authority | Activity can obtain its initial ordinary Rules authority from a mutable or immutable schema-shaped browser global before the first canonical outcome | Candidate `679532d` rejected; classic-script globals cannot prove provenance | Browser Rules is an inert facade; one lexical trusted composition captures the private Rules adapter before any match starts |
| V112-102 | P1 | Events/Rules authority | Rules lazily accepts a frozen schema-shaped `FlipgameV112EventKernel` global; a forgery can capture the private match capability and force a terminal Plinko win | Immutable export hardening is insufficient; lexical composition required | Browser EventKernel is inert; private Rules/EventKernel instances compose lexically and forged globals never receive a match capability |
| V112-103 | P1 | Progression/Public projection | Public Runtime `refresh` and mutation/import results can return the raw active `MatchClaimTokenV1`, including its private nonce | Candidate `d9f97de` rejected by independent audit | Every public return/event crosses one recursive redaction boundary; only private reward authority can observe the bearer token |
| V112-104 | P2 | Progression/Notification projection | A redacted subscriber event clears the active reservation but leaves `matchClaimCursor` ahead of `consumedMatchOrdinal`, publishing an impossible state | Candidate `d9f97de` rejected by independent audit | Result states use the same canonical public projection as snapshots and exports |
| V112-105 | P2 | Progression/Receipt identity | A token behind the consumed frontier is reported duplicate even when its match ID, activity and nonce differ from the persisted receipt | Candidate `d9f97de` rejected by independent audit | Duplicate recognition is bound to the exact durable receipt identity; unrelated stale tokens fail closed |
| V112-106 | P2 | Runtime/Metadata integrity | `OwnerTestIntegrationV1` is shallow-frozen while its `registeredActivityIds` array remains mutable | Candidate `d9f97de` rejected by independent audit | Recursively freeze children before the parent and reject mutation in browser/CommonJS tests |
| V112-107 | P2 | Events/Alien compatibility | Alien mode currently suppresses the v1.12 event registry, while legacy/unadapted effects are visually weak or physically nonsensical under zero gravity | Dedicated 30-event compatibility pass required | One explicit Alien adapter per event; obvious zero-gravity physical impact, hazard difficulty, missability, UFO continuity and exact cleanup/profile restoration |

| V112-108 | P1 | Plinko/Physical commands | Contact commands can change position, mass, force or impulse outside the expected phase constraints | Kernel owner remediation active | Launch, step and contact share one phase-aware command validator; physical travel cannot be substituted with caller metadata |
| V112-109 | P2 | Plinko/Timing | Elapsed-only phase progression can claim a full drop with tiny integration steps and an unchanged body position | Kernel owner remediation active | Trusted fixed ticks plus observed motion, canonical board geometry and sensor contact establish the phase/result |
| V112-110 | P2 | Plinko/Camera | Correct camera cues can accompany a conflicting target or renderer override | Kernel owner remediation active | Renderer consumes one validated bottle-centroid tracking plan; no jackpot targeting or independent override |
| V112-111 | P1 | Plinko/Live integration | Live startPlinko still uses eight rows and replaces the selected collider with a circle; trampoline/kernel are not connected | Open live integration | Same selected Flipper, physical trampoline, canonical 24-row board, verified timing and all nine visible reward slots |
| V112-112 | P1 | Profile/Native Web Locks | Actual Chromium rejects navigator.locks.request when ifAvailable and signal are combined; simulated locks missed the browser failure | Corrected in 385e195; native Chrome and runtime regression passed, release-browser QA pending | Immediate probe omits signal; queued acquisition remains abortable and closed callbacks cannot acquire authority |
| V112-113 | P1 | Landing/Engine adapter | Real engine may validate an upright make at the settling deadline while private Landing authority permits only a deadline miss | Fixed (`3a3e9c8`, `6117892`); release-browser QA pending | One approved deadline rule across engine, authority and driver; no silent forced verdict or stuck match |
| V112-114 | P1 | Plinko/Preserved collider | A real Matter corpus using the preserved Bottle-scale compound collider bridges/rests on the 9px canonical upper pegs; every observed run reaches the no-contest boundary despite physical recovery impulses | Corrected in isolated Matter adapter; 81/81 compound-Flipper corpus resolves across all nine slots, live integration/independent QA pending | Selected Flipper remains the real body with authored parts/dynamics; shared tethered contact chassis and measured anti-balance forces resolve physically, then clean up completely |
| V112-115 | P1 | Setup/Unlock authority | A fresh profile unlocks `bottle`, `desk-gyroscope` and `mechanical-metronome` on the v1.11 ladder while the private v1.12 authority grants only `bottle` at level 1, so the second seat defaults to a Flipper that authority refuses: Battle cannot open at all and a classic match plays to the end and earns nothing | Fixed (`b08d1eb`); the picker asks the reward authority too | Every Flipper the setup screen offers is one the private authority accepts, verified in a real browser before a Battle opens |
| V112-116 | P2 | Battle/Live lane presentation | An alternating relay invited the next competitor's flick while the previous bottle was still lying where it fell, repainted in the new competitor's colour, and only snapped it upright at the instant of the flick | Fixed (`fa1fbbf`) | The table is set when the lane becomes a competitor's: upright bottle, their Flipper and colour, fresh arena seed, and never reset while a flip is in the air |

| V112-117 | P2 | Battle/Composition source | An edit left two copies of `cancelBattle` and `abandonBattle` in `js/v112-browser-entry.js`; hoisting meant the second pair ran, but the dead first copy still read the removed `battle.totalFlips`, so nothing said which body the bundle carried | Fixed (`65d3c56`); the build now refuses an entry that declares a function twice | The reward authority has exactly one body per function, enforced when the bundle is composed rather than discovered by reading it |
| V112-118 | P1 | Battle/Live lane geometry | A resize or phone rotation mid-Battle left the physics world at its entry size and the lane rectangles with it: measured in Chromium, 900×800 → 1400×620 kept `groundY` at 552 in a 620-tall viewport with the bottle 250px left of centre, and every later flick was qualified against a lane that had moved | Fixed (`b2b2281`, `a5bb8b1`) | The lane re-fits the world and the host re-aims the runtime on every reported resize; a flip already in the air keeps the world it was launched into |
| V112-119 | P1 | Battle/Host teardown order | `abandon()` and `cancel()` tore the lane down before the private authority agreed to release the reservation, so a refusal — a finalizing series may not be left — took the table away from a Battle that could then be neither finished nor left | Fixed (`a5bb8b1`) | The authority decides first; a refused release leaves the lane exactly as it was |
| V112-120 | P1 | Battle/Second series | Playing a Battle, leaving and starting another put "Battle is waiting for a valid host update." on the screen and marked the route unavailable while the new heat ran underneath it: the host retired the finished series only after asking the application to reserve the next one, and reserving emits | Fixed (`0b7a3e1`) | The host retires a series before it asks for the next reservation, and the route treats a host with no series yet as the screen opening rather than a broken host |
| V112-121 | P2 | Battle/Result presentation | A finished series showed "Battle complete." under a live heat-and-volley heading, still told people to wait for their active lane, left a lane-sized empty box where the table had been, and never said who won — the route projected scores and lanes but not the winner the rules had already named | Fixed (`b7f7e7b`) | The projection carries the winner, the heading announces it, live-play chrome stands down, and the stage stands down once the series stops taking launches |

Art directions listed in `docs/v112-plan.md` are product-approval tasks rather
than defects until an implementation violates a frozen invariant. The original
Bottle/no-face and original T-Rex/no-change rules are release-blocking invariants.
